[build-system]
requires = ["setuptools>=45", "wheel", "requests>=2.25.0"]
build-backend = "setuptools.build_meta"

[project]
name = "spacejar"
version = "0.1.0"
description = "Binary installer for Spacejar CLI"
requires-python = ">=3.7"
dependencies = ["requests>=2.25.0"]